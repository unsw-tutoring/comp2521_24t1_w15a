#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#include "Graph.h"

// returns the number of vertices that are reachable from src
int numReachable(Graph g, int src) {
    // TODO
    return 0;
}
