#include <stdio.h>
#include <stdlib.h>

struct node {
	int value;
	struct node *next;
};

// return the sum of nodes in a linked list, given its head
// ! use a while loop
int listSum(struct node *list) {
	// TODO
	return 0;
}

// return the sum of nodes in a linked list, given its head
// ! use a for loop
int listSum(struct node *list) {
	// TODO
	return 0;
}
